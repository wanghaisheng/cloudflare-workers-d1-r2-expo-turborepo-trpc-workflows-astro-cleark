{
  "name": "@acme/apiservice",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0 --remote",
    "build": "tsc",
    "deploy": "wrangler publish",
    "clean": "git clean -xdf .cache .turbo node_modules .wrangler",
    "test": "vitest",
    "test:local": "vitest --config vitest.config.local.ts"
  },
  "dependencies": {
    "@acme/db": "workspace:*",
    "@acme/trpc": "workspace:*",
    "typescript": "catalog:",
    "wrangler": "3.93.0"
  },
  "devDependencies": {
    "@acme/eslint-config": "workspace:*",
    "@acme/tsconfig": "workspace:*",
    "@types/better-sqlite3": "^7.6.8",
    "@types/node": "^20.16.11",
    "better-sqlite3": "^9.2.2",
    "vitest": "^1.6.0"
  }
}